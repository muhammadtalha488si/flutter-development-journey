# Dart Programming Language 🎯

## About Dart

- **Object-oriented** — everything is an object, including numbers and functions.
- **Class-based** — supports interfaces, mixins, and abstract classes.
- **Strongly/statically typed** — with type inference and optional dynamic typing.
- **Garbage collected** — automatic memory management.
- **Compiled** — can be compiled to native machine code (AOT) or JavaScript.

---

## We Use Dart Because

| Reason | Description |
|--------|-------------|
| 🚀 Fast Development | Hot reload lets you see changes instantly without restarting the app. |
| 📱 Cross-Platform | Write once, run on Android, iOS, Web, Windows, macOS, and Linux. |
| ⚡ High Performance | Compiles to native ARM/x64 machine code for fast startup and execution. |
| 🛡️ Type Safety | Sound null safety helps catch null-reference errors at compile time. |
| 🧩 Easy to Learn | Familiar C-style syntax — easy for developers coming from Java, JavaScript, C#, or Kotlin. |
| 🌍 Strong Ecosystem | Backed by Google with a rich package ecosystem via pub.dev. |

---

## Features

- ✅ Sound null safety
- ✅ Strong typing with type inference (`var`, `final`, `const`)
- ✅ Asynchronous programming (`async`/`await`, `Future`, `Stream`)
- ✅ Object-oriented with classes, mixins, interfaces, and extensions
- ✅ Generics support
- ✅ Pattern matching (switch expressions, records)
- ✅ JIT (Just-In-Time) compilation for fast development cycles
- ✅ AOT (Ahead-Of-Time) compilation for optimized production builds
- ✅ Built-in package manager (`pub`)
- ✅ Rich standard library (`dart:core`, `dart:async`, `dart:io`, `dart:math`, etc.)

## Use Cases

- 📱 **Mobile Apps** — via Flutter (Android & iOS)
- 🌐 **Web Apps** — compiled to JavaScript
- 🖥️ **Desktop Apps** — Windows, macOS, Linux via Flutter Desktop

---

## Dart Tools

| Tool | Purpose |
|------|---------|
| `dart` | Run, compile, and manage Dart programs |
| `pub` | Dependency/package manager |
| `dartfmt` / `dart format` | Code formatter |
| `dartanalyzer` / `dart analyze` | Static code analysis |
| `dartdoc` | Generates documentation from comments |
| `dart compile` | Compile to native executable or JS |

---

